apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.workspace.name }}-pvc
spec:
  storageClassName: {{ .Values.workspace.store.storageClassName }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.workspace.store.size }}
